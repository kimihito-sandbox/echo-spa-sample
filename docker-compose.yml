version: '3.7'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
      target: build
    volumes:
      - ./:/go/app
    environment:
      - ENV=development
    command: go run main.go
    ports:
      - 1323:1323
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    command: yarn start
    volumes:
      - ./web/:/web
    ports:
      - 3000:3000
